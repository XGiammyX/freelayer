/**
 * @freelayer/relay — placeholder. NO IMPLEMENTATION (Phase 4+ / Gate D).
 *
 * When it exists, the relay is an OPTIONAL, self-hostable, UNTRUSTED blind
 * courier: it stores and forwards opaque ciphertext capsules and must never
 * become required infrastructure (ADR-0001).
 *
 * Nothing here binds ports, serves HTTP/WebSocket, or touches a database —
 * and nothing may, until the relay protocol design (blinded pickup
 * addressing) exists and passes review.
 */

export const RELAY_STATUS = "placeholder_no_implementation_phase_4" as const;
