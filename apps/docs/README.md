# @freelayer/docs

**Status: static placeholder page — no build step, no assets, no analytics.**

Documentation site rendering the contents of [`docs/`](../../docs/). Static output, self-hostable.

Constraints: no analytics, no external fonts/scripts, no tracking of any kind — the documentation site follows the same privacy rules as the product.
